# Frontend standalone local dev environment template
# Copy to frontend/.env when running Vite directly.
# Local development API base URL
VITE_API_URL=http://localhost:5000/api

# Docker build injects VITE_API_URL=/api through docker-compose.yml
